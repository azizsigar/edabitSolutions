const numbers = [1, 2, 3, 4, 5];

// Array içindeki her bir sayıyı iki katına çıkaran bir arrow fonksiyonu
const doubledNumbers = numbers.map(number => number * 2);

console.log(doubledNumbers); // Çıktı: [2, 4, 6, 8, 10]
